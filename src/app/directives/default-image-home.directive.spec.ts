import { DefaultImageHomeDirective } from './default-image-home.directive';

describe('DefaultImageHomeDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultImageHomeDirective();
    expect(directive).toBeTruthy();
  });
});
